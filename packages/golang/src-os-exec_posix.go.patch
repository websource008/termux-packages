diff -u -r ../go/src/os/exec_posix.go ./src/os/exec_posix.go
--- ../go/src/os/exec_posix.go	2024-03-29 15:27:02.000000000 +0000
+++ ./src/os/exec_posix.go	2024-05-24 22:23:22.855862054 +0000
@@ -51,6 +51,11 @@
 		sysattr.Files = append(sysattr.Files, f.Fd())
 	}
 
+	if runtime.GOOS == "android" {
+		argv = append([]string{name}, argv...)
+		name = "@TERMUX_ANDROID_LINKER@"
+	}
+
 	pid, h, e := syscall.StartProcess(name, argv, sysattr)
 
 	// Make sure we don't run the finalizers of attr.Files.
